# Car Editor 3D Viewer

## INSTALL

- npm install
- resources > files > unzip car.zip into resources/files/car/
- npm scripts => build-all
- npm scripts => server-start
- in browser => apps > bmweditor